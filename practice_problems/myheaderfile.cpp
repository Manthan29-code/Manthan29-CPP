#include<iostream.h>
#include<fact.h>  // save in bin folder
using namespace std;

int main()
{
    int num;
    cout<<"Enter a integer to find factorial of it"<<endl;
    cin>>num;
    cout<<"factroil of "<<num<<" is = "<<factorial(num); 
    return 0;
}
